# 2026-06-17（三）Streamlit 打磨 + 截图

## 目标

把 Streamlit 从"能跑"升级到"能演示"：观感、过程感、可解释性。

## 任务

- [x] **开工前基线确认**：使用 `main` / `origin/main` 的当前合并结果作为
  06-17 基线，而不是继续旧的 `claude/ci-eval-fixes` 分支；已从
  `main` 创建并切换到 `codex/2026-06-17-streamlit-polish`。
- [x] **运行过程可视化**：用 `st.status` / progress 展示当前节点
  （intake → research → retrieve → rerank → generate → validate → render），
  让 demo 时观众能看到 agent 在"做事"。
  （实现提示：`app.build_graph().stream(...)` 按节点产出更新状态，比 invoke 更适合 UI。）
  已实现：Streamlit 使用 `build_graph().stream(..., stream_mode="updates")`
  驱动 `st.status` 和 `st.progress`，并保留 CLI `run_workflow` 路径。
- [x] **证据面板**：侧栏或 expander 展示 web/kb 证据列表（标题、来源链接、
  rerank 分数），点引用编号能对上。
  已实现：侧栏证据面板按 Web / KB 分组展示引用 ID、标题、来源链接/路径、
  rerank 分数和摘录；计划正文中的 `[web-N]` / `[kb-N]` 与面板 ID 对齐。
- [x] **结果展示**：plan.md 渲染 + `st.download_button` 下载 plan.json / plan.md。
  已实现：成功页展示计划摘要、统一下载 plan.md / plan.json / evidence.json，
  并渲染 plan.md 预览；失败页也会展示可用下载产物和运行状态 JSON。
- [x] **告警与降级**（接 06-11 的 warnings）：黄色告警条、证据计数、校验失败红色横幅。
  已实现：运行后展示 web/kb 证据计数，warnings 使用黄色告警条，
  最终校验失败时显示红色横幅和校验错误详情。
- [x] **输入体验**：JD 文本框 + 示例 JD 一键填充按钮（demo 时省打字）。
  已实现：`填充示例 JD` 会写入字节跳动 / AI Agent 工程师 /
  `examples/jd_agent_engineer.txt`。
- [x] 截图 3-4 张存 `docs/img/`：输入页、运行中、结果页、证据面板。
  已新增：`streamlit-input.png`、`streamlit-running.png`、
  `streamlit-result.png`、`streamlit-evidence-panel.png`。

## 验收

- 全流程在 UI 上有过程感，无"白屏等待 1 分钟"。
- 截图素材就位，README 可直接引用。
- CLI 路径回归不破：`uv run python -m src.cli plan ...` 仍正常。

## 备注

- 不引入额外前端依赖，st 原生组件够用；时间省下来留给明天的 README。
- 2026-06-17 第二项验证：`uv run python -m py_compile app.py` 通过；
  `uv run python -c "import app"` 可导入（bare Streamlit warning 可忽略）；
  Browser 打开 `http://127.0.0.1:8501/` 页面可渲染并响应空表单校验；
  `uv run pytest` 通过 42 个测试，1 个 live 测试按预期跳过。
- 2026-06-17 证据面板验证：`uv run python -m py_compile app.py` 通过；
  helper 断言确认 web/kb 示例证据会渲染引用 ID、来源链接/路径和四位小数分数；
  Browser 页面烟测无 console error，空表单校验仍正常；
  `uv run pytest` 通过 42 个测试，1 个 live 测试按预期跳过。
- 2026-06-17 收尾验证：`uv run python -m py_compile app.py` 通过；
  helper 断言确认下载按钮和告警/校验失败 UI 可渲染；`uv run pytest`
  通过 42 个测试，1 个 live 测试按预期跳过；Browser 真实 UI 流程验证
  示例填充、运行中状态、成功结果、下载按钮和证据面板，且无 console error；
  CLI smoke：`uv run python -m src.cli plan --company "字节跳动" --role
  "AI Agent 工程师" --jd-file examples/jd_agent_engineer.txt --out
  runs/cli_smoke` 成功生成 plan.md / plan.json。
