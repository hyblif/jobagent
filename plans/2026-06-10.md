# 2026-06-10（三）技术债清理

## 目标

落实今天评审确定的三项决定，让代码、文档、依赖三者一致。

## 任务

- [x] **reranker 收敛到 sentence-transformers CrossEncoder**
  - 改 `src/rag/rerank.py`：`_load_reranker()` 改用 `sentence_transformers.CrossEncoder`，
    `compute_score(pairs, normalize=True)` 等价写法为 `predict(pairs)` + sigmoid
    （`CrossEncoder(..., default_activation_function=torch.nn.Sigmoid())` 或对输出手动 sigmoid）。
  - `pyproject.toml` 删除 `FlagEmbedding`，跑 `uv lock` 更新锁文件。
  - 测试不用动（都在 `rerank()` 函数层 mock），但跑一遍确认：`uv run pytest`。
  - 手动 smoke：`uv run python -c "from src.rag.rerank import rerank; print(rerank('RAG 是什么', [{'excerpt': 'RAG 即检索增强生成'}, {'excerpt': '今天天气不错'}], top_k=2))"`，确认相关片段分数更高。
- [x] **模型默认值统一为 deepseek-v4-flash**
  - `src/llm.py:14` 默认值 `deepseek-chat` → `deepseek-v4-flash`。
  - `.env.example` 和本地 `.env` 同步改。
- [x] **uv.lock 入库 + 文档同步**
  - `.gitignore` 删掉 `uv.lock` 行，提交锁文件。
  - `PROJECT_PLAN.md` §3 依赖管理行改为：uv + pyproject.toml + uv.lock 为主，
    `requirements.txt` 由 `uv export` 生成作为 fallback；§3 Reranker 行补一句 CrossEncoder 实现。
  - `requirements.txt` 重新生成：`uv export --no-dev --no-hashes > requirements.txt`。
- [x] 全量回归：`uv run pytest`（30 个全过）+ 提交一次 commit。

## 验收

- `grep FlagEmbedding pyproject.toml` 无结果；`uv run pytest` 全绿。
- `git ls-files | grep uv.lock` 有结果。
- 新开 shell 不设 `DEEPSEEK_MODEL` 时，`get_llm()` 的 model 是 `deepseek-v4-flash`。

## 备注

- CrossEncoder 首次加载会重新下载模型权重（bge-reranker-v2-m3 约 2GB），留出下载时间；
  已设 `RERANKER_MODEL_PATH` 的话本地路径直接可用。

## 完成记录

- 2026-06-10：`src/rag/rerank.py` 已切换到 sentence-transformers `CrossEncoder` + sigmoid 输出；`pyproject.toml`、`uv.lock`、`requirements.txt` 已移除 `FlagEmbedding`。
- 2026-06-10：`src/llm.py`、`.env.example`、本地 `.env` 默认模型统一为 `deepseek-v4-flash`。
- 2026-06-10：已运行 `uv lock`、`uv export --no-dev --no-hashes --output-file requirements.txt`、`uv run pytest`（30 passed）和 reranker smoke；新 shell 不设 `DEEPSEEK_MODEL` 时 `get_llm()` 返回 `deepseek-v4-flash`。
