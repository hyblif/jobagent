# 2026-06-12（五）检索质量：重排查询增强 + 语料扩充 I

## 目标

让重排查询贴住 JD 的具体技术栈，并开始把语料库扩到 eval 可用的规模。

## 任务

- [x] **重排查询增强**（`src/agent/nodes.py` rerank_node）
  - 现状：固定模板 `"{role} {company} 面试 高频考点"`，JD 技术栈没参与。
  - 改法：intake 抽出的 `search_queries` 里的技术关键词拼进重排查询，
    或 intake 节点额外产出 `kb_query` 字段（推荐，语义更清晰）。
  - 对比验证：拿一份真实 JD，肉眼对比改前/改后 top-6 kb 证据的相关性，记录在本文件底部。
- [x] **评估 per-query 重排是否值得**：当前是所有候选合并后对一个复合查询重排一次。
  试一版"每个 search_query 单独重排取 top-2 再合并去重"，对比效果与延迟，择优保留（不强求换）。
- [x] **语料扩充 batch 1**（目标 +4 个主题文件，每个 15-25 个 Q&A 块）
  - `data/baguwen/network.md`（TCP/HTTP/HTTPS/DNS 等）
  - `data/baguwen/database.md`（MySQL 索引/事务/锁、Redis）
  - `data/baguwen/concurrency.md`（线程/协程/GIL/锁）
  - `data/baguwen/system_design.md`（缓存/消息队列/限流等常见题）
  - 格式遵循现有文件的 heading/Q&A 结构（ingest 按 heading 切块）；
    `SOURCES.md` 登记每个文件的整理来源。
- [x] 重建索引：`uv run python scripts/build_index.py`，确认块数明显增长。

## 验收

- 重排查询包含 JD 技术关键词（trace 或日志可见）。
- `data/baguwen/` ≥ 8 个主题文件；索引块数记录在本文件底部。
- `uv run pytest` 全绿。

## 备注

- 语料是自己整理改写的要点而非原文粘贴，遵守 PROJECT_PLAN §5 的语料策略。
- 写语料本身也是面试复习——优先写自己目标岗位（AI Agent 工程师）权重高的主题。

## 完成记录（2026-06-13）

- `intake_node` 现在要求 LLM 输出 `kb_query`；失败或缺失时从 JD、岗位、search_queries 中抽取技术关键词生成兜底查询。
- `rerank_node` 已改用 `kb_query`。示例 JD 下旧查询为 `Agent 工程师 阿里巴巴 面试 高频考点`，新查询包含 `agent / llm / rag / embedding / reranker / langgraph / python / redis` 等关键词。
- 改前 top-6 混入网络/泛机器学习块；改后 top-6 主要集中在 `llm_rag.md`、`llm_agent.md` 的 RAG、Agent、LangGraph、reranker 主题。
- per-query rerank 实验：延迟约 1.253s（模型 warm 后），但首个公司面试流程 query 容易引入网络泛化块；未稳定优于 composite `kb_query`，按计划不进入主实现。
- batch 1 已新增 `network.md`、`database.md`、`concurrency.md`、`system_design.md`，每个 20 个 Q&A 块，并更新 `SOURCES.md`。
- 重建索引结果：`Found 162 chunks`，collection `baguwen` count 为 162。
- 验证：`uv run pytest` -> 37 passed。
