# 2026-06-11（四）鲁棒性：告警上报 + 降级提示

## 目标

消灭"静默吞异常"的 demo 风险：任何一条腿（Tavily / KB / LLM）失效时，
用户在 CLI 和 Streamlit 都能看到明确提示，而不是拿到一份零引用的泛泛计划。

## 任务

- [ ] `AgentState` 增加 `warnings: list[str]` 字段。
- [ ] 改造静默 except：
  - `intake_node`：LLM 关键词提取失败 → warning "intake 降级为模板检索词"。
  - `research_node`：Tavily 异常 → warning 带异常类型；记录实际拿到的 web 证据数。
  - `retrieve_node` / `src/rag/retriever.py`：collection 不存在或为空 → warning "知识库为空，建议先 build_index"。
  - `plan_node`：保留现有错误回灌逻辑，但 LLM 调用失败也追加进 warnings。
- [ ] `tools/web_search.py`：加超时（如 15s）和一次重试；失败抛出而非返回空，让 research_node 统一捕获记 warning。
- [ ] CLI（rich）：运行结束后打印证据计数（web N 条 / kb M 条）+ warnings 黄色面板。
- [ ] Streamlit：顶部 `st.warning` 显示 warnings；侧边栏显示证据计数；plan 校验未通过时显眼红色横幅。
- [ ] 测试：新增 3-4 个单测——Tavily 全挂、KB 为空、两者都挂时 workflow 仍跑完且 warnings 正确。

## 验收

- 故意填错 `TAVILY_API_KEY` 跑 CLI：流程不崩，输出含"web 证据 0 条"和对应 warning。
- 删掉 `.chroma/` 跑 Streamlit：页面出现知识库为空告警。
- `uv run pytest` 全绿（预计 33-34 个）。

## 备注

- warnings 进 state 而不是直接 print，是为了 LangSmith trace 里也能看到（后面 06-16 验证）。
