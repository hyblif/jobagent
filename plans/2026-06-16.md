# 2026-06-16（二）LangSmith 验证 + live smoke

## 目标

可观测性从"装了依赖"变成"真的能看"：完整 trace + 截图素材；真实 API 端到端跑通。

## 任务

- [x] **LangSmith 启用与验证**
  - `.env` 设 `LANGSMITH_TRACING=true` + 有效 `LANGSMITH_API_KEY`。
  - 跑一次完整 workflow，到 LangSmith 控制台确认：7 个节点都有输入输出、
    validate→generate 重试环在 trace 里可见（可临时把 MAX_RETRIES 触发条件做成
    必失败一次来演示，截完图还原）。
  - 截图 2-3 张存 `docs/img/`：全图 trace、单节点输入输出、重试环。
    - 已用 Chrome 登录态替换为真实 LangSmith 控制台截图：`docs/img/langsmith-trace-success.png`、
      `docs/img/langsmith-node-io-intake.png`、`docs/img/langsmith-retry-loop.png`。
- [x] **live smoke 测试**
  - `tests/test_live_smoke.py`：`RUN_LIVE_TESTS=1` 且 key 齐全时才跑
    （`pytest.mark.skipif`），真实 Tavily + DeepSeek 全链路一次，断言产物文件存在
    且 plan.json 可被 PrepPlan 解析。
  - 跑一次：`RUN_LIVE_TESTS=1 uv run pytest tests/test_live_smoke.py -v`。
- [x] **计时**：live 跑一次记录端到端耗时（验收标准是 3 分钟内），各节点耗时从
  LangSmith 读，记录在本文件底部。瓶颈若是 LLM 多轮重试，检查 prompt。
- [x] 修 live 跑暴露的一切问题（真实 API 返回格式、超时、编码等）。

## 验收

- LangSmith 上有一条完整成功 trace + 一条含重试的 trace。
- live smoke 测试存在且通过；端到端 < 3 分钟。
- 截图素材就位。

## 备注

- 确认 langchain 环境变量兼容性：新版用 `LANGSMITH_*`，旧版文档多写 `LANGCHAIN_*`，
  以实际生效为准（跑完去控制台看有没有 trace 即可验证）。

## 验证记录

- Branch: `codex/2026-06-16-langsmith-live-smoke`
- Offline suite: `uv run pytest` -> 42 passed, 1 skipped in 2.32s（live smoke 默认跳过）。
- Live smoke: `RUN_LIVE_TESTS=1 uv run pytest tests/test_live_smoke.py -v` -> 1 passed in 73.36s。
- Live output assertions: `plan.json` / `plan.md` / `evidence.json` all existed; `plan.json`
  parsed by `PrepPlan`; final state had a plan; evidence was non-empty; elapsed time < 180s.
- LangSmith success trace:
  - Project: `jobagent`
  - Trace ID: `019ed54a-e4f0-7872-8997-cd429fada74b`
  - UTC time: 2026-06-17 11:15:15.824206 to 11:16:27.841399
  - Nodes verified from SDK metadata:
    `intake -> research -> retrieve -> rerank -> generate -> validate -> render`
  - Error: false
- LangSmith retry trace:
  - Trace ID: `019ed54d-d343-7eb1-8ea1-0952e788a65d`
  - UTC time: 2026-06-17 11:18:27.907932 to 11:18:27.980639
  - Nodes verified from SDK metadata:
    `intake -> research -> retrieve -> rerank -> generate -> validate -> generate -> validate -> render`
  - `generate` runs: 2; `validate` runs: 2; Error: false
- Screenshot material:
  - `docs/img/langsmith-trace-success.png`
  - `docs/img/langsmith-node-io-intake.png`
  - `docs/img/langsmith-retry-loop.png`
  - 2026-06-17 later update: Chrome extension access with an authenticated LangSmith session
    replaced the earlier SDK-derived placeholders with real console screenshots.
