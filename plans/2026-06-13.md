# 2026-06-13（六）语料扩充 II + 索引质检

## 目标

语料库达到 eval 可用规模（10 个主题文件、150+ 块），并确认 ingest 切块质量。

## 任务

- [x] **语料扩充 batch 2**（目标 +2~3 个文件）
  - `data/baguwen/llm_rag.md`（RAG/embedding/reranker/向量库——既是语料又是你自己的项目谈资）
  - `data/baguwen/ml_basics.md`（过拟合/评估指标/微调 vs RAG 等）
  - 视余力补 `data/baguwen/git_linux.md` 或目标公司相关的工程实践主题。
- [x] **ingest 质检**
  - 随机抽 10 个块，检查：标题 metadata 正确、块内容完整（没把一个 Q&A 切成两半）、
    无超长块（> 1000 字的块考虑在 ingest 里二次切分）。
  - 检查重复/近重复块（同一问题在两个文件都写了的情况，保留更好的一份）。
- [x] 重建索引并做人工 spot-check：挑 5 个典型面试问题跑
  `retrieve(q, use_rerank=True)`，肉眼确认 top-3 相关性，结果记录在本文件底部。
- [x] 顺手修 spot-check 暴露的明显问题（切块参数、metadata 缺失等）。

## 验收

- `data/baguwen/` ≥ 10 个主题文件，索引总块数 ≥ 150。
- 5 个 spot-check 问题至少 4 个 top-3 含正确答案块。
- `SOURCES.md` 覆盖所有文件。

## 备注

- 今天结束后语料冻结到 eval 完成为止——eval 期间改语料会让数字不可比。

## 完成记录（2026-06-13）

- batch 2 已新增 `llm_rag.md`、`ml_basics.md`、`git_linux.md`，每个 20 个 Q&A 块，并更新 `SOURCES.md` 与 `.gitignore` allowlist。
- 当前 `data/baguwen/` 共 10 个主题文件，ingest 统计为 162 个 chunk；无超过 1000 字的 chunk，最大 chunk 370 字。
- 随机抽检 10 个 chunk：`source/title/heading/chunk_type` metadata 均完整，未发现 Q&A 被切断。
- 重建索引命令：`uv run python scripts/build_index.py --data-dir data/baguwen --persist-dir .chroma/jobagent`；结果为 162 chunks indexed。
- 5 个 spot-check 问题 top-3 命中情况：RAG 切块/reranker -> 命中 `llm_agent.md`/`llm_rag.md`；MySQL 最左前缀 -> 命中 `database.md`；TCP 三次握手 -> 命中 `network.md`；asyncio/协程 -> 命中 `python.md`/`concurrency.md`；缓存击穿/雪崩 -> 命中 `database.md`/`system_design.md`。结果 5/5 命中。
- Chroma telemetry 仍会打印 `capture() takes 1 positional argument but 3 were given` warning，但索引构建和查询均成功，不影响当前验收。
